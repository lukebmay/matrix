# Secrets (contextual names only)

Track **names + where found** — never real values. Goal: lift secrets out of the tree over time.

## Password nicknames (confirmed safe)

These appear in docs as **nicknames**, not live secrets. Agents: flag unknowns
first; after human confirm, leave and label safe. See `agents/security.md`.

| Nickname | Where | Status |
| --- | --- | --- |
| `7zSecure` | `~/.ssh/README.txt` (ssh.7z archive hint) | **Safe — nickname** (2026-07-14, human confirmed) |

## Real secret names (no values)

| Name | Location / notes |
| --- | --- |
| *(none recorded)* | |
